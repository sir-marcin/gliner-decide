# Direct dependencies. Python >= 3.10 required (gliner2 needs it).
# Everything else (torch, transformers, huggingface-hub, ...) comes in as a
# transitive dependency of gliner2[local].
gliner2[local]==2.0.0
protobuf
sentencepiece
fastapi==0.141.1
uvicorn[standard]==0.53.0
